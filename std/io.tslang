namespace std.io;

extern puts(str): int;
extern printf(str, ..): int;
extern scanf(str, ..): int;

/*
 Print text in the console.
 */
export fn write(str text): void {
	printf(text);
}

/*
 Print a question in the console and prompt
 the user for input.
 */
export fn prompt(str question): str {
	printf(str + "? ");
	scanf("%s", &[str response]);
	
	return response;
}

/*
 Print text followed by a new line in the console.
 */
export fn ln(str text): void {
	puts(text);
} 
